name: bottom_navigation_bar
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  commons:
    path: ../../commons
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  cart:
    path: ../cart
  beamer: ^1.7.0
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  freezed: ^3.2.5
  mocktail: ^1.0.4
  bloc_test: ^10.0.0
  # Solo por `aProduct()`: el badge se testea con un carrito real, y `Product`
  # vive en `catalog`, que `cart` no reexporta.
  test_support:
    path: ../../test_support

flutter:
